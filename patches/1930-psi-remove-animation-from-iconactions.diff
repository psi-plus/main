--- psi.orig/src/widgets/iconaction.cpp
+++ psi/src/widgets/iconaction.cpp
@@ -165,7 +165,8 @@ void IconAction::setPsiIcon(const PsiIco
 	if ( i ) {
 		d->icon = new PsiIcon(*i);
 		connect(d->icon, SIGNAL(iconModified()), SLOT(iconUpdated()));
-		d->icon->activated(true);
+		//We newer use animated iconactions
+		//d->icon->activated(true);
 
 		is = d->icon->icon();
 	}
