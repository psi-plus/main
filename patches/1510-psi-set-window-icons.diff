Entering 'iris'
Entering 'src/libpsi'
diff --git a/src/aboutdlg.cpp b/src/aboutdlg.cpp
index c41b5ce..dc0cb51 100644
--- a/src/aboutdlg.cpp
+++ b/src/aboutdlg.cpp
@@ -24,12 +24,14 @@
 
 #include "applicationinfo.h"
 #include "aboutdlg.h"
+#include "iconset.h"
 
 AboutDlg::AboutDlg(QWidget* parent)
 	: QDialog(parent)
 {
 	setAttribute(Qt::WA_DeleteOnClose);
 	ui_.setupUi(this);
+	setWindowIcon(IconsetFactory::icon("psi/psiplus_logo").icon());
 
 	setModal(false);
 
diff --git a/src/accountmanagedlg.cpp b/src/accountmanagedlg.cpp
index b69be46..a26bd85 100644
--- a/src/accountmanagedlg.cpp
+++ b/src/accountmanagedlg.cpp
@@ -41,6 +41,7 @@
 #include "psicontactlist.h"
 #include "iconaction.h"
 #include "shortcutmanager.h"
+#include "iconset.h"
 
 using namespace XMPP;
 
@@ -94,6 +95,7 @@ AccountRemoveDlg::AccountRemoveDlg(const UserAccount &acc, QWidget *parent)
 :QDialog(parent)
 {
 	setupUi(this);
+	setWindowIcon(IconsetFactory::icon("psi/account").icon());
 	setModal(false);
 #if QT_VERSION >= 0x040500
 	setWindowFlags(Qt::Dialog | Qt::WindowTitleHint | Qt::WindowMinimizeButtonHint | Qt::WindowCloseButtonHint | Qt::CustomizeWindowHint);
diff --git a/src/activitydlg.cpp b/src/activitydlg.cpp
index 3a9d83e..c5426e5 100644
--- a/src/activitydlg.cpp
+++ b/src/activitydlg.cpp
@@ -33,7 +33,9 @@ ActivityDlg::ActivityDlg(QList<PsiAccount*> list) : QDialog(0), pa_(list)
 	if(pa_.isEmpty())
 		close();
 	ui_.setupUi(this);
+	setWindowIcon(IconsetFactory::icon("activities/other").icon());
 	setModal(false);
+
 	connect(ui_.cb_general_type, SIGNAL(currentIndexChanged(const QString&)), SLOT(loadSpecificActivities(const QString&)));
 	connect(ui_.pb_cancel, SIGNAL(clicked()), SLOT(close()));
 	connect(ui_.pb_ok, SIGNAL(clicked()), SLOT(setActivity()));
diff --git a/src/avcall/calldlg.cpp b/src/avcall/calldlg.cpp
index c921414..f29318d 100644
--- a/src/avcall/calldlg.cpp
+++ b/src/avcall/calldlg.cpp
@@ -26,6 +26,7 @@
 #include "common.h"
 #include "psiaccount.h"
 #include "psioptions.h"
+#include "iconset.h"
 
 // from opt_avcall.cpp
 extern void options_avcall_update();
@@ -63,6 +64,7 @@ public:
 	{
 		ui.setupUi(q);
 		q->setWindowTitle(tr("Voice Call"));
+		q->setWindowIcon(IconsetFactory::icon("psi/avcall").icon());
 
 		ui.lb_bandwidth->setEnabled(false);
 		ui.cb_bandwidth->setEnabled(false);
diff --git a/src/bookmarkmanagedlg.cpp b/src/bookmarkmanagedlg.cpp
index c09a0f1..2400055 100644
--- a/src/bookmarkmanagedlg.cpp
+++ b/src/bookmarkmanagedlg.cpp
@@ -26,6 +26,7 @@
 
 #include "bookmarkmanager.h"
 #include "psiaccount.h"
+#include "iconset.h"
 
 BookmarkManageDlg::BookmarkManageDlg(PsiAccount* account)
 	: QDialog()
@@ -37,6 +38,8 @@ BookmarkManageDlg::BookmarkManageDlg(PsiAccount* account)
 	setWindowFlags(Qt::Dialog | Qt::WindowTitleHint | Qt::WindowMinimizeButtonHint | Qt::WindowMaximizeButtonHint | Qt::WindowCloseButtonHint | Qt::CustomizeWindowHint);
 #endif
 	ui_.setupUi(this);
+	setWindowIcon(IconsetFactory::icon("psi/bookmarks").icon());
+
 	account_->dialogRegister(this);
 
 	QAction* removeBookmarkAction = new QAction(this);
diff --git a/src/contactmanager/contactmanagerdlg.cpp b/src/contactmanager/contactmanagerdlg.cpp
index 68d3c09..2776875 100644
--- a/src/contactmanager/contactmanagerdlg.cpp
+++ b/src/contactmanager/contactmanagerdlg.cpp
@@ -44,6 +44,7 @@ ContactManagerDlg::ContactManagerDlg(PsiAccount *pa) :
 	ui_.setupUi(this);
 	pa_->dialogRegister(this);
 	setWindowTitle(tr("Contacts Manager")+" - "+pa->jid().bare());
+	setWindowIcon(IconsetFactory::icon("psi/action_contacts_manager").icon());
 
 	um = new ContactManagerModel(this, pa_);
 	um->reloadUsers();
diff --git a/src/geolocationdlg.cpp b/src/geolocationdlg.cpp
index 6adaae5..df98cc8 100644
--- a/src/geolocationdlg.cpp
+++ b/src/geolocationdlg.cpp
@@ -27,6 +27,7 @@
 #include "pepmanager.h"
 #include "geolocation.h"
 #include <QLineEdit>
+#include "iconset.h"
 
 GeoLocationDlg::GeoLocationDlg(QList<PsiAccount*> list) : QDialog(0), pa_(list)
 {
@@ -34,7 +35,9 @@ GeoLocationDlg::GeoLocationDlg(QList<PsiAccount*> list) : QDialog(0), pa_(list)
 	if(pa_.isEmpty())
 		close();
 	ui_.setupUi(this);
+	setWindowIcon(IconsetFactory::icon("system/geolocation").icon());
 	setModal(false);
+
 	connect(ui_.pb_cancel, SIGNAL(clicked()), SLOT(close()));
  	connect(ui_.pb_ok, SIGNAL(clicked()), SLOT(setGeoLocation()));
 	connect(ui_.pb_reset, SIGNAL(clicked()), SLOT(reset()));
diff --git a/src/groupchatdlg.cpp b/src/groupchatdlg.cpp
index 5784408..d4fc015 100644
--- a/src/groupchatdlg.cpp
+++ b/src/groupchatdlg.cpp
@@ -634,7 +634,7 @@ GCMainDlg::GCMainDlg(PsiAccount *pa, const Jid &j, TabManager *tabManager)
 	setAcceptDrops(true);
 
 #ifndef Q_OS_MAC
-	setWindowIcon(IconsetFactory::icon("psi/groupChat").icon());
+	setWindowIcon(IconsetFactory::icon("psi/start-chat").icon());
 #endif
 
 	ui_.setupUi(this);
@@ -1858,7 +1858,7 @@ void GCMainDlg::setLooks()
 
 	// update the widget icon
 #ifndef Q_OS_MAC
-	setWindowIcon(IconsetFactory::icon("psi/groupChat").icon());
+	setWindowIcon(IconsetFactory::icon("psi/start-chat").icon());
 #endif
 
 	ui_.lv_users->setLooks();
@@ -2097,6 +2097,7 @@ void GCMainDlg::doBookmark()
 	formLayout->addRow(tr("&Auto join:"), chkAJoin);
 	layout->addLayout(formLayout);
 	layout->addLayout(blayout);
+	dlg->setWindowIcon(IconsetFactory::icon("psi/bookmark_remove").icon());
 	dlg->setLayout(layout);
 	dlg->setMinimumWidth(300);
 	dlg->connect(saveBtn, SIGNAL(clicked()), dlg, SLOT(accept()));
diff --git a/src/mooddlg.cpp b/src/mooddlg.cpp
index 1e9101d..67220f7 100644
--- a/src/mooddlg.cpp
+++ b/src/mooddlg.cpp
@@ -34,7 +34,9 @@ MoodDlg::MoodDlg(QList<PsiAccount*> list)
 	if(pa_.isEmpty())
 		close();
 	ui_.setupUi(this);
+	setWindowIcon(IconsetFactory::icon("mood/").icon());
 	setModal(false);
+
 	connect(ui_.pb_cancel, SIGNAL(clicked()), SLOT(close()));
 	connect(ui_.pb_ok, SIGNAL(clicked()), SLOT(setMood()));	
 
diff --git a/src/mucjoindlg.cpp b/src/mucjoindlg.cpp
index f78872e..4779797 100644
--- a/src/mucjoindlg.cpp
+++ b/src/mucjoindlg.cpp
@@ -30,6 +30,7 @@
 #include "psicontactlist.h"
 #include "groupchatdlg.h"
 #include "bookmarkmanager.h"
+#include "iconset.h"
 
 MUCJoinDlg::MUCJoinDlg(PsiCon* psi, PsiAccount* pa)
 	: QDialog(0)
@@ -40,6 +41,7 @@ MUCJoinDlg::MUCJoinDlg(PsiCon* psi, PsiAccount* pa)
 #endif
 	setModal(false);
 	ui_.setupUi(this);
+	setWindowIcon(IconsetFactory::icon("psi/groupChat").icon());
 	controller_ = psi;
 	account_ = 0;
 	controller_->dialogRegister(this);
diff --git a/src/options/optionsdlg.cpp b/src/options/optionsdlg.cpp
index ca35aa9..3731108 100644
--- a/src/options/optionsdlg.cpp
+++ b/src/options/optionsdlg.cpp
@@ -499,6 +499,7 @@ OptionsDlg::OptionsDlg(PsiCon *psi, QWidget *parent)
 	: QDialog(parent)
 {
 	setupUi(this);
+	setWindowIcon(IconsetFactory::icon("psi/options").icon());
 	pb_apply = buttonBox->button(QDialogButtonBox::Apply);
 
 	setAttribute(Qt::WA_DeleteOnClose);
diff --git a/src/privacy/privacydlg.cpp b/src/privacy/privacydlg.cpp
index 905a1bb..dc6edd5 100644
--- a/src/privacy/privacydlg.cpp
+++ b/src/privacy/privacydlg.cpp
@@ -26,6 +26,7 @@
 #include "privacylist.h"
 #include "privacymanager.h"
 #include "privacylistmodel.h"
+#include "iconset.h"
 
 // fixme: subscribe on the destroyed() signal of the manager
 
@@ -34,6 +35,7 @@ PrivacyDlg::PrivacyDlg(const QString& account_name, PrivacyManager* manager, QWi
 	ui_.setupUi(this);
 	setAttribute(Qt::WA_DeleteOnClose);
 	setWindowTitle(tr("%1: Privacy Lists").arg(account_name));
+	setWindowIcon(IconsetFactory::icon("psi/eye").icon());
 #if QT_VERSION >= 0x040500
 	setWindowFlags(Qt::Dialog | Qt::WindowTitleHint | Qt::WindowMinimizeButtonHint | Qt::WindowMaximizeButtonHint | Qt::WindowCloseButtonHint | Qt::CustomizeWindowHint);
 #endif
diff --git a/src/profiledlg.cpp b/src/profiledlg.cpp
index 021c084..acb9157 100644
--- a/src/profiledlg.cpp
+++ b/src/profiledlg.cpp
@@ -81,6 +81,7 @@ ProfileOpenDlg::ProfileOpenDlg(const QString &def, const VarList &_langs, const
 	setupUi(this);
 	setModal(true);
 	setWindowTitle(CAP(windowTitle()));
+	setWindowIcon(IconsetFactory::icon("psi/profile").icon());
 	pb_open->setDefault(true);
 
 	langs = _langs;
diff --git a/src/psioptionseditor.cpp b/src/psioptionseditor.cpp
index fd6a8f0..67712c1 100644
--- a/src/psioptionseditor.cpp
+++ b/src/psioptionseditor.cpp
@@ -184,6 +184,9 @@ PsiOptionsEditor::PsiOptionsEditor(QWidget *parent)
 		pb_detach->setToolTip(tr("Open a detached option editor window."));
 		buttonLine->addWidget(pb_detach);
 		connect(pb_detach, SIGNAL(clicked()), SLOT(detach()));
+	} else {
+		setWindowIcon(IconsetFactory::icon("psi/advanced-plus").icon());
+		setWindowTitle(CAP(tr("Advanced")));
 	}
 
 
diff --git a/src/statusdlg.cpp b/src/statusdlg.cpp
index 4fe6d26..319e140 100644
--- a/src/statusdlg.cpp
+++ b/src/statusdlg.cpp
@@ -71,6 +71,7 @@ StatusShowDlg::StatusShowDlg(const UserListItem &u)
 	te->setText(u.makeDesc());
 
 	setWindowTitle(tr("Status for %1").arg(JIDUtil::nickOrJid(u.name(), u.jid().full())));
+	setWindowIcon(IconsetFactory::icon("psi/action_direct_presence").icon());
 	resize(400,240);
 
 	pb->setFocus();
@@ -112,6 +113,7 @@ StatusSetDlg::StatusSetDlg(PsiCon *psi, const Status &s, bool withPriority)
 	d->withPriority = withPriority;
 
 	setWindowTitle(CAP(tr("Set Status: All accounts")));
+	setWindowIcon(IconsetFactory::icon("psi/action_direct_presence").icon());
 	d->setStatusMode = setStatusForAccount;
 	init();
 }
@@ -128,6 +130,7 @@ StatusSetDlg::StatusSetDlg(PsiAccount *pa, const Status &s, bool withPriority)
 	d->withPriority = withPriority;
 
 	setWindowTitle(CAP(tr("Set Status: %1").arg(d->pa->name())));
+	setWindowIcon(IconsetFactory::icon("psi/action_direct_presence").icon());
 	d->setStatusMode = setStatusForAccount;
 	init();
 }
diff --git a/src/tipdlg.cpp b/src/tipdlg.cpp
index afbcf85..1af9a83 100644
--- a/src/tipdlg.cpp
+++ b/src/tipdlg.cpp
@@ -27,6 +27,7 @@
 
 #include "psioptions.h"
 #include "psicon.h"
+#include "iconset.h"
 
 void TipDlg::show(PsiCon* psi)
 {
@@ -48,6 +49,7 @@ TipDlg::TipDlg(PsiCon* psi)
 {
 	setAttribute(Qt::WA_DeleteOnClose);
 	setupUi(this);
+	setWindowIcon(IconsetFactory::icon("psi/tip").icon());
 	setModal(false);
 
 	psi_->dialogRegister(this);
diff --git a/src/xmlconsole.cpp b/src/xmlconsole.cpp
index ca3ad0e..bd20f32 100644
--- a/src/xmlconsole.cpp
+++ b/src/xmlconsole.cpp
@@ -32,6 +32,7 @@
 #include "psiaccount.h"
 #include "psicon.h"
 #include "psicontactlist.h"
+#include "iconset.h"
 
 //----------------------------------------------------------------------------
 // XmlConsole
@@ -40,7 +41,7 @@ XmlConsole::XmlConsole(PsiAccount *_pa)
 :QWidget()
 {
 	ui_.setupUi(this);
-
+	setWindowIcon(IconsetFactory::icon("psi/xml").icon());
 	pa = _pa;
 	pa->dialogRegister(this);
 	connect(pa, SIGNAL(updatedAccount()), SLOT(updateCaption()));
