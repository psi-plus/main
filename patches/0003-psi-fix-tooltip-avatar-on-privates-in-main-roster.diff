--- git.orig/src/psiaccount.cpp
+++ git/src/psiaccount.cpp
@@ -5919,6 +5919,7 @@ void PsiAccount::invokeGCChat(const Jid
 	u->setInList(false);
 	u->setName(j.resource());
 	u->setPrivate(true);
+	u->setAvatarFactory(avatarFactory());
 
 	// make a resource so the contact appears online
 	UserResource ur;
